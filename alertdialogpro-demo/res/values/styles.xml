<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat"></style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="styledAlertDialogTheme">@style/AppTheme.StyledAlertDialog</item>
    </style>

    <style name="AppTheme.StyledAlertDialog" parent="AlertDialogTheme.Base.StyledAlertDialog"></style>

</resources>